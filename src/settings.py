# 入力ファイル名と出力フォルダ名
data_csv_name = "sample.csv"
result_dir_name = "sample"

# 入力CSVのどの行・列にどの情報があるか指定
column_band_name = 2
columns_possible_schedules = list(range(9,14))
columns_members = list(range(3,8))
columns_info = [2,1,3,4,5,6,7,14]

max_exec_minutes = 30

